angular.module('snippetsApp', ['ngAnimate', 'ui.bootstrap', 'ngRoute']);

/*.config(['$routeProvider', function($routeProvider) {
    $routeProvider.
      when('/cinemarkCityMall', {
        templateUrl: 'views/cinemarkCityMall.html',
        controller: 'CinemarkCityMallCtrl'
      }).
      otherwise({
        redirectTo: '/landing'
      });
}]);*/

